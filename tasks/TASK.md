# TASK — активное задание (архитектор -> кодер)

Статус: **нет активного задания.**

## Предыдущее задание — ВЫПОЛНЕНО (2026-09-07)
Развернуть трек A (whisper-local) на RTX 4060 + GPU — сделано и проверено вживую.
Подробности и грабли: `tasks/REPORT.md`, `PROJECT_LOG.md` (2026-09-07), `AGENTS.md` (трек A).
Итог: large-v3 на GPU (float16, ru), хоткей `ctrl+space`, распознавание ~0.7 с.

## Что дальше — трек B (ещё не начат)
Своя диктовка на облачной модели Google (распознавание + зачистка текста в один проход).
Первый шаг при старте: **проверить в сети актуальную модель Google и её цену** (облачные
модели быстро меняются) — только потом архитектор пишет полноценный TASK.
Ключ Gemini — в `app/.env` (`GEMINI_API_KEY`), не коммитить (AGENTS.md, трек B).

## Не делать
- Ничего не удалять по своей инициативе (DEV-NOTES §15).
- git commit/push — только по явной просьбе.
