# CLAUDE.md

Тонкий редирект (Claude Code читает этот файл нативно).

- Полный устав кодера: **`AGENTS.md`**.
- Общий канон среды: файл **DEV-NOTES.md** (путь даёт Юрий в начале сессии — на разных ПК он свой, абсолютный путь здесь намеренно не фиксируется).
- Порядок чтения в начале сессии: `CONTEXT.md` -> `AGENTS.md` -> `tasks/TASK.md`.
